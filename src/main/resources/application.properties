# 应用服务 WEB 访问端口
server.port=8080

# 数据库连接信息
spring.datasource.url=jdbc:mysql://localhost:3306/snowisland?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=KzhKzhTones4848
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.hibernate.ddl-auto=update
# 关闭 Hibernate SQL 刷屏（需要调试时再设为 true）
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# 需要时在日志级别单独打开 SQL（推荐不要用 println）
# logging.level.org.hibernate.SQL=DEBUG

